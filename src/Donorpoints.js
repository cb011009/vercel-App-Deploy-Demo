/*import React from "react";
import { useState } from "react";
import "./CSS.css";
import Dropdown from "./Dropdown";
import Backbutton from "./Backbutton";
export default function DonorPoints() {
   /* return (
      <div>
      <h1>DONOR POINTS</h1>
        <p>DONOR POINTS EARNED ARE:</p>
        <p>XXX</p>
      </div>
    );*/
   /*
    const [selectedOption, setSelectedOption] = useState("");

    const handleSelectChange = (event) => {
      setSelectedOption(event.target.value);
    };
  
    return (
      <div>
         <h1>FIND NEAREST BLOOD BANK</h1>
         <h3>SELECT THE NEAREST DISTRICT TO YOUR LOCATION</h3>
        <select value={selectedOption} onChange={handleSelectChange}>
          <option value="">Select an option</option>
       {/* Add more options here *//*}
        </select>
      </div>
    );
    const [selectedOption, setSelectedOption] = useState("");

    const handleSelect = (option) => {
      setSelectedOption(option);
    };
 
      return (
        <div className="container">
          <div className="image-container">
          </div>
          <div className="info-container">
            <h4 className="font-link1">You have selected <span className="info">xxxxxxx</span> as your closest district</h4>
            <h4 className="font-link2">These are the blood banks available</h4>            
            <Dropdown
        title="Select an option"      
        items={["Option 1", "Option 2", "Option 3"]}
        onSelect={handleSelect}
      />     
          </div>
      
      <Backbutton/>
      </div>
       
      );
    }*/
    
   
  